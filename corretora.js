const prompt = require (prompt-Sync)

const db = [];

let proxId = 1;

const model = (id = ) =>{
    const nome = prompt("Nome:  ");




        if(nome != " ") {
            return {
                id,
                nome,
            }
        };

        console.log ("dados invalidos")
            const store = () =>{
                const novo
            }
}
